--path:"."

# The sibling projects are developed alongside acc, so they resolve from the
# working copies next to this repository rather than from an installed copy.
# acc.nimble declares them by git URL for checkouts that do not have them.
--path:"../../pitchfork/src"
--path:"../../arena_context_store/src"
